- error = @author.errors.include?(:name)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :name, :class => 'control-label'
  .controls
    =f.text_field :name, :class => 'input-xlarge input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:name, :class => 'text-error') : pat(:example)
- error = @author.errors.include?(:email)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :email, :class => 'control-label'
  .controls
    =f.text_field :email, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:email, :class => 'text-error') : pat(:example)
- error = @author.errors.include?(:kind)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :kind, :class => 'control-label'
  .controls
    =f.text_field :kind, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:kind, :class => 'text-error') : pat(:example)
- error = @author.errors.include?(:drupal_uid)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :drupal_uid, :class => 'control-label'
  .controls
    =f.text_field :drupal_uid, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:drupal_uid, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:authors, :index), :class => 'btn'
